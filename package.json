{
  "name": "eslint",
  "version": "1.0.0",
  "description": "All-in-one vector tile server backend.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
  }
}
